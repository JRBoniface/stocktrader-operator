# Static Helm values for the External Secrets Operator.
# These values are environment-agnostic and apply to all overlays.
# Environment-specific configuration (tenantId, vaultUrl, etc.) is
# injected via replacements in the overlay kustomization.yaml.
installCRDs: true
webhook:
  networkPolicy:
    enabled: false
certController:
  enabled: true
